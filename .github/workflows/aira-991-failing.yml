name: AIRA-991 failing workflow

on:
  pull_request:
    types:
      - opened
      - synchronize
      - ready_for_review
  workflow_dispatch:

jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - name: Emit QA marker
        run: echo "AIRA-991 advanced PR triggers QA"
      - name: Fail intentionally
        run: exit 1
