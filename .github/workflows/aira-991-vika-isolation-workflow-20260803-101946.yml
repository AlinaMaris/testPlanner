name: AIRA-991 smoke isolation 20260803-101946
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  fail:
    name: isolation fail
    runs-on: ubuntu-latest
    steps:
      - run: echo "isolation failure" && exit 1
